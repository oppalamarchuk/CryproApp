<UserControl x:Class="CryproApp.Views.ConvertView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryproApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Text="From:" FontWeight="Bold" Margin="0,0,0,5"/>

                <TextBox Text="{Binding FromCurrency,UpdateSourceTrigger=PropertyChanged}"
              FontSize="16"/>
                <TextBox Text="{Binding FromAmount,UpdateSourceTrigger=PropertyChanged}"
              FontSize="16" Margin="0,10,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <TextBlock Text="To:" FontWeight="Bold" Margin="0,0,0,5"/>

                <TextBox Text="{Binding ToCurrency,UpdateSourceTrigger=PropertyChanged}"
              FontSize="16"/>
                <TextBox Text="{Binding ToAmount,UpdateSourceTrigger=PropertyChanged}"
              FontSize="16" 
              Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
       
        <TextBlock Grid.Row="1" Text="Enter name of the currency (e.g. ultima, bitcoin, m2, ethereum)"/>
    </Grid>
</UserControl>
