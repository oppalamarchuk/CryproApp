<UserControl x:Class="CryproApp.Views.CoinDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CryproApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding CoinDetails.Name}"
                   Grid.Row="0"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="10"/>
        <TextBlock Text="{Binding CoinDetails.Symbol}"
                   Grid.Row ="1"/>
        <TextBlock Text="{Binding CoinDetails.Data.Price, StringFormat=C}"
                   Grid.Row="1"
                   HorizontalAlignment="Right"
                   Margin="0,0,10,0"/>
    </Grid>
</UserControl>
