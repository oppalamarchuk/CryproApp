<UserControl x:Class="CryproApp.Views.CoinDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CryproApp.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
               Text="{Binding CoinDetails.Name, TargetNullValue=Loading...}"
               FontSize="24"
               FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
               Text="{Binding CoinDetails.Symbol}" />

        <TextBlock Grid.Row="2"
               Text="{Binding CoinDetails.Data.Price,
                      StringFormat={}{0:$0.##########}}"/>
        
        <lvc:CartesianChart Grid.Row="3">
            <lvc:CartesianChart.Series>
                <lvc:LineSeries 
                    Values="{Binding Prices}"
                    PointGeometry="{x:Null}"
                    Fill="Transparent"/>
            </lvc:CartesianChart.Series>

            <lvc:CartesianChart.AxisY>
                <lvc:Axis 
                    MinValue="{Binding MinPrice}"
                    MaxValue="{Binding MaxPrice}"/>
            </lvc:CartesianChart.AxisY>

            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Time" Labels="{Binding TimeLabels}" />
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>

        <ListBox Grid.Row="4"
                 ItemsSource="{Binding CoinDetails.Markets}"
                 HorizontalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{Binding Name}"/>
                        <TextBlock Grid.Row="1" Text="{Binding Volume}"/>
                        <TextBlock Grid.Row="2" Text="{Binding Price,
                                      StringFormat={}{0:0.##########}}"/>
                        
                        <Button Grid.Row="3"
                                Content="Visit"
                                Command="{Binding DataContext.OpenMarketCommand,
                                          RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding TradeUrl}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>

</UserControl>
