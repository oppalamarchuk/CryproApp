<Window x:Class="CryproApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CryproApp.ViewModels"
        xmlns:views="clr-namespace:CryproApp.Views"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0"
            HorizontalAlignment="Left" Margin="10,10,0,0">

            <Button Content="Top Coins"
                Command="{Binding OpenCoinsCommand}"
                Height="30"
                Margin="10"/>
            
            <Button Content="Search"
                Command="{Binding OpenSearchCommand}"
                Height="30"
                Margin="10"/>

            <Button Content="Convert"
                    Command="{Binding OpenConvertCommand}"
                    Height="30"
                    Margin="10"/>
        </StackPanel>
        
        <ContentControl Content="{Binding CurrentViewModel}"
                        Grid.Row="1">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:CoinsViewModel}">
                    <views:CoinsView/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:CoinDetailsViewModel}">
                    <views:CoinDetailsView/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:SearchCoinViewModel}">
                    <views:SearchCoinView/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:ConvertVeiwModel}">
                    <views:ConvertView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
